
// import loan from "../models/loan.js";
// import user from "../models/user.js";
// import book from "../models/book.js";

// const createLoan = async (data) => {
//     const { userId, bookId, dataEmprestimo, dataPrevistaDevolucao, dataDevolucao, status, multa } = data;

//     if (!userId || !bookId || !dataEmprestimo || !dataPrevistaDevolucao || !dataDevolucao || !status || !multa) {
//         const error = new Error("userId,bookId,dataEmprestimo,dataPrevistaDevolucao,dataDevolucao,status e multa são");
//         error.statusCode = 400;
//         throw error;
//     }

//     const user = await user.findById(userId);

//     if (!user) {
//         const error = new Error("Usuário não encontrado");
//         error.statusCode = 404;
//         throw error;
//     }



// }
